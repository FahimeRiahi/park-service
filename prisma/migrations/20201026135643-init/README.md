# Migration `20201026135643-init`

This migration has been generated by fahimeriahi@gmail.com at 10/26/2020, 5:26:43 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "park-depot"."User" (
"id" SERIAL,
"username" text   NOT NULL ,
"first_name" text   ,
"last_name" text   ,
"address" text   ,
"mobile" text   ,
"email" text   ,
"password" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "park-depot"."WhitelistCar" (
"id" SERIAL,
"license_plate" text   NOT NULL ,
"allowed_from_date" timestamp(3)   NOT NULL ,
"allowed_to_date" timestamp(3)   NOT NULL ,
"allowed_time_from" text   NOT NULL ,
"allowed_time_to" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "User.username_unique" ON "park-depot"."User"("username")

CREATE UNIQUE INDEX "WhitelistCar.license_plate_unique" ON "park-depot"."WhitelistCar"("license_plate")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201026133205-init..20201026135643-init
--- datamodel.dml
+++ datamodel.dml
@@ -2,10 +2,31 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
+
+model User {
+  id       Int     @default(autoincrement()) @id
+  username    String  @unique
+  first_name     String?
+  last_name     String?
+  address     String?
+  mobile     String?
+  email     String?
+  password String
+}
+
+model WhitelistCar{
+  id       Int     @default(autoincrement()) @id
+  license_plate    String  @unique
+  allowed_from_date DateTime
+  allowed_to_date DateTime
+  allowed_time_from String
+  allowed_time_to String
+
+}
```


